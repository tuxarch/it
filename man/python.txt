virtualenv

более удобная обертка virtualenvwrapper

bashrc:
export VIRTUALENVWRAPPER_PYTHON=/usr/bin/python3
source /usr/bin/virtualenvwrapper.sh

mkvirtualenv env-name
workon - список проектов
workon - заходим в окружение
deactive
rmvirtualenv env-name

ставим пакеты через pip или спец файл
pip freeze > requirements.txt
pip install -r requirements.txt

---------------------------------------------------------

PIP
upgrade all packages:
pip install -U $(pip freeze | awk -F'[=]' '{print $1}')



print 	
	end " " - позоляет указать разделитель конца строки
	sep - устанавливает разделитель между переменными

input() ввод данных в консоли
	int (input()) - переопределить строку в число.

Типы данных 	float	
		integer
		string

sorted - сортировка 
split - разделить

type - указывает на тип

Пошаговое заполнение переменных. Переменной нет в программе если до ее
определения дело еще не дошло. 

	Брек-поинт и пошаговое выполенение.

Синтаксис - корректная последовательность символов.(соответствие языку)
Семантика - описание задачи с помощью синтаксиса, вкладывание
смысла.(соответствие задачи)
Прагматика - применение семантики в реальных условиях.(соответствие жизни) 
Алгоритм - последовательность действий.

Трансляция - перевод кода на язык машинных команд.
	Исходный и объектные коды
	Комплилятор - программа-транслятор
Интерпретация - это динамическая трансляция.

Смешанная реализация - код сначала транслируется для разметки памяти и
размещения в ней переменных, подключения дополнительных библиотек.
А потом в процессе выполенния достается из соостветствующих объектов.


Объект - единица данных в программе.

Виртуализация - реализация абрактных объектов с помощью машинных команд.

_____________________________________________

Работа со строками

