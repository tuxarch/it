Установка:

pip install awscli
pacman -S aws-cli

complete -C aws_completer aws

aws configure
__________________________
Сервисы:

ec2 - virtual servers
vpc - network
s3 backet - web spaces
route52 - domain
rds,dynamodb - sql
cloudformation - ias


AIM - сервис для разграничения прав
_________________________
S3:

Создать s3
bucket s3
aws s3 mb s3://file.format  --region=eu-central
aws s3 cp file s3://file.format
_________________________

EC2:

