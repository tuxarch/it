программа берет ввод из файла или консоли разбивает и передает ввод другой
программе
$ find . -name "*.sh"| xargs rm -rf - классика использования
$ ls | xargs -p -l gzip - сжать все файлы
$ tr -dc A-Za-z0-9_ < /dev/urandom | head -c 10 | xargs - генерация пароля
