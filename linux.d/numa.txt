Данная архитектура широко используется на серверах
Обращание к части ОЗУ через другое ядро ЦП.

numactl --hardware - просмотр весов маршрутов к другой озу
numactl --cpunodebind=0 --membind=o ls - привязать процесс к ядру и банки
памяти

Равномерное распределение памяти
numactl --interleave all nginx
