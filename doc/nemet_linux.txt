К задачам админа относится:
	управление пользователями
	настройка и приобретение оборудования
	резервное копирование
	настройка и приобретение ПО
	мониторинг системы
	поиск неисправностей
	ведение документации в тч касающиеся изменения кофигурации и
	резервирования
	безопастность
	помощь пользователем в эксплуатации ЭВМ и ПО

Необходимо знать: bash, vi, python

	Linux реализует POSIX под лицензией GNU GPL, но также есть другие системы
реализуемые этот же стандарт под другими менее свободными лицензиями
OpenSolaris, FreeBSD. Выбор между ними в зависит от набора программ.
	Актуальность и поддержка дистрибьютива в ближайшее время.

RHEL и CentOS платная и бесплатная версия.
SUSE и OpenSUSE

Solaris, HP-UX, AIX

YaST2 smit - центр конфигурирования системы.

Мануалы хранятся в /usr/share/man

#which - показывает место расположения программы
#whereis - где есть упоминания в файловой системе о программе

Потоки ввода, вывода и ошибка
>& - перенаправляет все потоки
2> - перенаправляет поток ошибок

#find / -name man - ищет все, что есть начиная с корня относящееся к man

&& выполнение последующей команды если будет выполненна первая

\ служит для переноса команды на следующую строку

#export - позволяет создавать глобальные переменные
veribl="s" создает переменную среды. $veribl - выводит ее

#sort - сортирует входные строки
#uniq - выводит уникальные строки
#wc - подсчет строк, символов, слов
#tee - копирование в два места одновременно
#head, tail - конец и начало файла
#grep - поиск текста

_______________________________________-
создание скрипта
#!/bin/bash

chmod +x
./filecsr
Аргументы командной строки $0 - имя скрипта

local - позволяет создавать локальную переменную




__________________________________________


#printf - аналог echo 
#read - может служить для организации ввода переменной


Загрузка в однопользовательском режиме происходит через граб, с дописыванием
параметров ядру.

Уровни выполнения init 7 уровней 
/etc/inittab - хранит инфо о рунлевалах
SMF - управление службами в solaris

управление процессами и файлами со стороны пользователя

/etc/shadow - хранит зашифрованные пароли

PAM - модули реализующие идентификацию
Kerberos - метод аутентификации реализуемый сторонними системами

Пароли на машинах и на серверах следует делать различными

Существует возможность отселживаинея использвания sudo привелегий.

использовать /bin/su /usr/bin/su чтобы не натолкнуться на алисы 
в пользовательской системе.

kill - использует коректное завершение kill -9 мокрое убийство

nice renice - изменение приоритета процесса от -20 до +19.

ps & top обращаются к файловой системе /proc

strace - служит для вывода информации о процессе.





































