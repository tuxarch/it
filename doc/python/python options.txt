type(ver) - позволяет определить тип переменной

s (not) in d - ищет переменную s в d

min(max)(s) - выводит, больший или меньший элемент



s = open('files', mod) - дескриптор доступа к файлу

.append(x) - добавляет х к последовательности.

.insert(i,x) - вставляет элемент х в промежуток i

.count(x) - считает количество повторяющихся элементов.

.reverse() - изменяет порядок элементов, но только в массиве.

.sort() - сортирует элементы по возрастанию, но может также быть прописано и
свое условие



     Выборка элементов массива, списка s[-1] - последный элемент
последовательности.

del - позволяет удалить элемент



dict - словарь, представляет из себя список вида (ключ-значение) {1: 'sfdsgf'}



Для работы с файлами предназначены функии:

read()

write()

readline() readlines()

close()

организовать процесс записи файла можно через for line in f1.readlines():

f2.write(lines)





       import urllibf1 = urllib.urlopen("
       [
        http://python.onego.ru
       ](http://python.onego.ru)
       ")


Такая способность позволяет скопировать содержимое из интернета.

Для того чтобы посмотреть список системных имен можно просмотреть системныю
библиотеку в питоне с помощью

import keyword

keyword.kwlist

Название переменной в которой начальный символ _ говорит нам о том, что это
локальная переменная.

Глобальная переменная задается с помощью модификатора global







