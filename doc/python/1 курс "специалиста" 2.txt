#! /usr/bin/env python - необходимый комментарий для линукс систем.

которая позволяте запуск скрипта без указания на интерпритатор



типы переменных

          фундаментальные типы - поддержка этих типов происходит ОС.

                              Целые типы

                              Указатели

          базовые типы,  (работающие на одном компиляторе)

                              Строка и тп.

          Пользовательские типы

                              Классы

          Библиотечные типы

                              Разработанные сторонними программистами.

          Стандартный тип

                              Библиотечный тип, разработанный совместно с
компилятором.



Целевая сборка - (proc.exe, proc.zip - исходные коды и спользованные
библиотеки, python.dll - интерпритатор питона. Так же возможна шифрование
исходного кода.

      Все типы данных в питоне стандартные, либо пользовательские.

           ** type ** \- позволяте узнать тип переменой.

               Питон умеет автоматически назначать тип переменной.

               u - добавленная при объявлении строки задает тип данных unicode

               Питон умеет работать со сколь угодно длинными числами.

Возможно сложение между собой строки и числа, но необходимо указание типа
переменной числа.



На питоне возможно несколько точек входа.

Питон может автоматически выбирать тип данных для переменной. Т.е. в нем
присутствует динамическая типизация.

Динамическая типизация  - не хороша и не плоха, Языки высокого уровня решают
задачу, на языке задачи. ( не много не верно)



s = x^2 - возведение в степень 2



Типы данных

int

long

unicode

str

float

bool - (true and false)

NoneType - особый тип данных, значит ничего.

 С яблоками на луне, он конечно махнул.

list - массив

tuple - картеж

dict - словарь



Объектно - ориентированное программирование

Класс, экземпляр класса,



del Y - удаление объекта

Сборка мусора - возникает тогда когда не хватает памяти, или система
простаивает



Вычисление идут всегда слево направо

     x = x[:3]+"d"+x[3:] - функция позволяет вставить символ в строку.





