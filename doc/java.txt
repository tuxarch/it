Типы данных примитивные:
целочисленные: byte, short, int, long
дробные: flot, double
буквенные: char
логический: boolean


При обьявлении 0, false, \u0000

final - константа значение задается при объявлении и больше не меняется.


Классы обертки для примитивных типов данных:
Integer, Double

Имена переменных не могут начинаться с символа и содержать пробелы

psvm +Tab 
soutp

//
/* */
/** */ - javadoc

Операторы и ||, или && 
жесткое условие которое обязует проверить все условия
или не жесткое

if
else if
else 

switch
case

переменная удаляется из памяти после выполнение метода в котором она была
использованна

static - доступен для всех объектов

конструктор - метод который вызывается при создании объекта
Он всегда есть

Чтобы задать конструктор нужно создать метод с темже именем что и класс

this - позволяет сослаться на объект этого класса

Массивы

String [] name
String nanme []

Должны иметь количество элементов

Если нет заданного количества элементов то ипользуется ArrayList

циклы
for (counter=0; counter <totalPlayers; counter++)

while 
	counter++

break - выйти из цикла
continue - продолжить с начала 

do while - код выполняется хотябы один раз


AWT SWING - графические пакеты

AWT - старая, SWING - новая

___________
Функция lambda - это анонимный класс содержащий один метод



maven
install, package - собрать программу
compile - скомплировать

mvn exec:java -Dexec.mainClass="com.app.HelloWorld" - запустить ult com.app -
это пакет Hello - имя класса

_____________

Интерфейс не является классом
