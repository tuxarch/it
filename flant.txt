Проблемы в микросервисах 
проблема трассировки
проблема проблема масштабирования


=== Statefull в kuber

Реляционные базы
Носкул
Объектные хранилища
Поисковые движки
in-memory базы
Системы очередей
Сервис дискавери


Механизмы реализующие HA:
Deployment, StatefullSet
PodAntiaffinity - запрет создавать поды на одном узле
PodDisruptionBudgets - убивать поды после создания

Fencing - убить машину мастера если она отвалилась и недоступна.
С помощью розетки, порта, API-провайдера

Все ходят в базы через прокси

Тип работы с данными, только пишем, только читаем, распределенная запись

NFS плохо, так как не обеспечивает консистентность, желательно использовать
объектное хранилище

Фейловер на базе это редкость

Локально монтируем папку к контейнеру на StatefullSet
Монтируем две папки и ручками переключаем мастер в случае падения, нужен
прокси

Мастер отдельно, реплика завернуты в контейнеры и разворачиваются по
необходимости в kuber


Кластер базы в statefullset на service discovery

Percona cloud native поддержка работы в облаке
Stolon использует etcd для настройки прокси

==== Extended Kubernetes

Lets Encrypt Plugin
Operator - расширения позволяющее разворачивать абстракции
Metrics API - Metrics server - реализация автоскейлинка

29 - расширений uспользует flant
Автоматизация реализуется через написания операторов

shell-operator - быстро пишем операторы на шеле

=== Управление проектами
Dev - важнее прода, для разработчиков

Слушай внимательно, потому что именно тебе придется, делать что-то за другого

Google meet - для видеочатов и ведения дежурств
Система документации - как система поиска в разных проектах

Свои системы мониторинга и управления проектами, если имеешь кучу разных
компаний.

